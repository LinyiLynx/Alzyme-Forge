# Data

- `raw/` — original CAZy / GT metadata sources
- `splits/` — train/val/test for the best-AUC ESMC run (`0601`)
- `gt_reference/` — GT reference materials
- `esmc_embeddings/` — precomputed ESMC residue embeddings (`manifest.csv` + `embeddings/*.pt`)
