# ESMC Embedding Drop-In Directory

Place the server-generated ESMC output here:

```text
manifest.csv
embeddings/
  <sequence_hash>.pt
```

The default configs in `SSH_esmc/configs/` read this location.
